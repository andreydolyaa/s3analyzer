

export const S3 = {
  msg: {
    blockPublicAcls: "BlockPublicAcls is disabled - public ACLs can be set",
    ignorePublicAcls:
      "IgnorePublicAcls is disabled - existing public ACLs are not ignored",
    blockPublicPolicy:
      "BlockPublicPolicy is disabled - public bucket policies can be attached",
    restrictPublicBuckets:
      "RestrictPublicBuckets is disabled — public policies are not restricted",
    protected: "fully protected from public access",
    notProtected: "public access misconfiguration detected"
  },
};
